http.get./checkNum:
  fn: checkNum
  authn: false
  params:
    - name: age
      in: query
      required: true
      schema:
        type: number

  responses:
    200:
      content:
        application/json:
          schema:
            type: string
